name: CI
on: [push]
jobs:
  build:
    runs-on: macOS-latest
    steps:
    - uses: actions/checkout@master
    - name: build
      run: |
        cd vwc_fng_tracker_fe
        yarn install
        yarn run test
